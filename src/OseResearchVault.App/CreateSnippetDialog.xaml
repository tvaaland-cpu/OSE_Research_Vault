<Window x:Class="OseResearchVault.App.CreateSnippetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Snippet"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight">
    <Grid Margin="16" MinWidth="480">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Document" FontWeight="SemiBold"/>
        <TextBox x:Name="DocumentTitleText" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Company" FontWeight="SemiBold"/>
        <ComboBox x:Name="CompanyCombo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" DisplayMemberPath="DisplayName" SelectedValuePath="Id" IsEditable="True" IsTextSearchEnabled="True"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Locator" FontWeight="SemiBold"/>
        <TextBox x:Name="LocatorText" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Top" Text="Snippet text" FontWeight="SemiBold"/>
        <TextBox x:Name="SnippetText" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" Height="140" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Created by" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Margin="0,0,0,8" VerticalAlignment="Center" Text="user"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save" Width="90" IsDefault="True" Click="Save_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
